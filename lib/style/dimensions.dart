import 'package:flutter/cupertino.dart';

class Dimension {
  // Radius
  static BorderRadius buttonRadius = BorderRadius.circular(50);

  // Padding
  static const double defaultPadding = 10.0;
  static const double paddingLarge = 20.0;
}
